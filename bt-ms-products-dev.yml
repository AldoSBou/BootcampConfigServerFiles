server:
  port: ${CUSTOMER.SERVER.PORT:8083}    
spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/bootcampbdproducts
      
  redis:
    host: localhost
    port: 6379
      
# Eureka Client Configuration
eureka:
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: ${EUREKA.SERVER.HOSTNAME:http://localhost:8761/eureka}
  instance:
    hostname: localhost
    prefer-ip-address: false